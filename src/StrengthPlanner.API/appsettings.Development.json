{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
  // ConnectionStrings:DefaultConnection se cita iz .NET user-secrets (van repo-a).
  // Podesi ga sa:
  //   dotnet user-secrets set "ConnectionStrings:DefaultConnection" \
  //     "Host=localhost;Port=5432;Database=strengthplanner;Username=postgres;Password=..." \
  //     --project src/StrengthPlanner.API
}
